.target
	&.ripple, .ripple
		overflow hidden
		position relative

		&::after
			background currentcolor
			content ''
			height 100%
			opacity 0
			position absolute
			right 0
			top 0
			transition all .3s
			width 100%
  
	&:hover
		&.ripple, .ripple
			&::after
				opacity .08

	&:focus
		&.ripple, .ripple
			&::after
				opacity .16

	&:active
		&.ripple, .ripple
			&::after
				opacity .32