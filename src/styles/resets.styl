html
	box-sizing border-box
	font-size 100%

*, *:before, *:after
	box-sizing inherit

body
	margin 0
	//overflow hidden

a
	color inherit
	text-decoration: none

h1, h2, h3, h4, h5, h6, p, button, input, ol, ul
	all unset

button
	cursor pointer